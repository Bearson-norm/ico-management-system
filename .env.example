# Salin ke .env dan sesuaikan. URL harus sama dengan user/sandi yang ADA di server PostgreSQL Anda.

# --- Lokal + Docker Compose (user dev / sandi devpassword — lihat docker-compose.yml) ---
DATABASE_URL_MTC="postgresql://postgres:Admin123@127.0.0.1:5432/mtc_db"
DATABASE_URL_GA="postgresql://postgres:Admin123@127.0.0.1:5432/ga_db"

# --- VPS / Postgres native (ganti USER, PASSWORD, port, nama DB sesuai server Anda) ---
# Contoh jika role Postgres Anda bernama postgres:
# DATABASE_URL_MTC="postgresql://postgres:SANDI_ANDA@127.0.0.1:5432/mtc_db"
# DATABASE_URL_GA="postgresql://postgres:SANDI_ANDA@127.0.0.1:5432/ga_db"
# Port DB bukan 5432? Ganti angka di URL (mis. 1357).

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="ganti-dengan-string-random-panjang"

# NEXTAUTH_URL="https://msic.moof-set.web.id"
# HOST=127.0.0.1
# PORT=1325

# --- Auto-Sync Odoo & Google Sheets (MTC SCM) + GA Audit Snapshot ---
# CRON_TOKEN dipakai oleh scripts/cron-sync.js dan scripts/cron-audit-ga.ts
# (audit: H-2 akhir bulan 00:00 WIB, geser jika libur → POST /api/ga/audit/generate via scripts/cron-audit-ga.ts).
# CRON_TOKEN="isi_dengan_token_keamanan_untuk_cron_di_vps"
# Promosi user GA pertama ke administrator:
#   node scripts/promote-ga-admin.js <username>
# SCM_SHEET_URL="https://docs.google.com/spreadsheets/d/xxxxxxxxxxxxxxxx/edit"
# ODOO_PASSWORD="sandi_atau_api_key_odoo"
# ODOO_DB="foom-production-5808833"
# ODOO_UID=34